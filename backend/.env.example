MONGODB_URI=mongodb://127.0.0.1:27017
MONGODB_DB=gatherly
PORT=8000
CLIENT_ORIGIN=http://localhost:3000,http://127.0.0.1:3000


# Production: serve the built frontend from the API's HTTPS origin.
# SERVE_FRONTEND=true
# Set only to the known number of reverse-proxy hops for your hosting provider.
# TRUST_PROXY_HOPS=1
